server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taxi
    username: postgres
    password: Segnal96
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: auto

  jpa:
    hibernate:
      default_schema: user_service
      ddl-auto: update
    show-sql: true
