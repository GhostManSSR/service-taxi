server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taxi
    username: postgres
    password: Segnal96

  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest