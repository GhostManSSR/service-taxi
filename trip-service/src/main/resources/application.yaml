server:
  port: 8082

spring:
  application:
    name: trip-service

  datasource:
    url: jdbc:postgresql://localhost:5432/taxi
    username: postgres
    password: Segnal96

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    org.springframework: INFO
